query Input {
  checkout {
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentCustomizations {
    metafield(namespace: "payment-customization", key: "function-configuration") {
      value, type
    }
  }
}